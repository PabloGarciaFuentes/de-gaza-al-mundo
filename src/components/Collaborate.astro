---
const defaultData = {
  title: "Tu ayuda llega directa",
  description: "Únete a nuestra comunidad en Telegram para coordinar tu ayuda directamente con quienes la necesitan. Sin intermediarios, directo al corazón de Gaza.",
  cta: {
    icon: "chat",
    label: "Unirse al Canal de Telegram"
  },
  disclaimer: "Unirse es gratuito y seguro. Respetamos tu privacidad.",
  telegramIcon: {
    icon: "send",
    bgColor: "bg-blue-50",
    textColor: "text-[#0088cc]"
  }
};

const { data = defaultData } = Astro.props;
---

<!-- CTASection -->
<section id="collaborate-section" class="bg-[#fcfafa] py-16 md:py-24 relative overflow-hidden">
  <!-- Decorative circle -->
  <div
    class="absolute -top-24 -right-24 w-96 h-96 bg-accent-green/5 rounded-full blur-3xl"
  >
  </div>
  <div
    class="absolute -bottom-24 -left-24 w-96 h-96 bg-[#931a24]/5 rounded-full blur-3xl"
  >
  </div>
  <div class="w-full max-w-7xl mx-auto px-4 sm:px-10 relative z-10">
    <div
      class="bg-white rounded-2xl shadow-xl border border-[#f0f0f0] p-8 md:p-16 text-center max-w-4xl mx-auto"
    >
      <div
        class={`w-16 h-16 ${data.telegramIcon.bgColor} ${data.telegramIcon.textColor} rounded-full flex items-center justify-center mx-auto mb-6`}
      >
        <!-- Simulating Telegram Icon style -->
        <span class="material-symbols-outlined" style="font-variation-settings: 'FILL' 1; font-size: 2rem;">{data.telegramIcon.icon}</span>
      </div>
      <h2
        class="text-[#171212] text-3xl md:text-5xl font-black leading-tight mb-6"
      >
        {data.title}
      </h2>
      <p
        class="text-[#6b6b6b] text-lg md:text-xl leading-relaxed max-w-2xl mx-auto mb-10"
      >
        {data.description}
      </p>
      <div class="flex flex-col sm:flex-row justify-center gap-4">
        <button
          class="flex items-center justify-center gap-3 rounded-lg h-14 px-8 bg-[#931a24] hover:bg-red-800 transition-all transform hover:-translate-y-1 text-white text-lg font-bold shadow-lg cursor-pointer"
        >
          <span class="material-symbols-outlined">{data.cta.icon}</span>
          <span>{data.cta.label}</span>
        </button>
      </div>
      <p class="mt-6 text-sm text-[#6b6b6b]">
        {data.disclaimer}
      </p>
    </div>
  </div>
</section>